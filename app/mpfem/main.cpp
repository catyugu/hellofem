// hellofem::app — mpfem application entry point (Phase B stub)
// SPDX-License-Identifier: MIT

#include <cstdio>

int main(int argc, char**)
{
    // Full driver lands in Phase F (reads clean Java + mesh, solves, exports).
    std::printf("mpfem: driver not yet implemented (Phase F); parsed %d args\n", argc);
    return 0;
}
